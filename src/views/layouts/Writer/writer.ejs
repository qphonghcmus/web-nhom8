<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <title>
    Writer - Admin Page 
  </title>
  <meta content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, shrink-to-fit=no' name='viewport' />
  <!--     Fonts and icons     -->
  <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700|Roboto+Slab:400,700|Material+Icons" />
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/latest/css/font-awesome.min.css">
  <!-- CSS Files -->
  <link href="../../public/vendors/css/writer/material-dashboard.css?v=2.1.1" rel="stylesheet" />
  <% for(var i=0; i < cssfiles.length; i++){%>
    <% var path = '../../public/resource/CSS/' + cssfiles[i] + '.css'; %>
    <link rel="stylesheet" href="<%=path%>">
  <%} %>
    <!-- sort and search js -->
    <% if( typeof(activeEdit) != 'undefined' && activeEdit === true){%> 
        <!-- <script src="../../public/resource/JS/writer-edit.js"></script> -->
    <%}else{ %>
        <script src="../../public/resource/JS/writer-sort&searchTable.js"></script>
    <%}  %>
    
    <% if( typeof(activePost) != 'undefined' && activePost === true){%> 
        <script src="https://cdn.ckeditor.com/4.11.4/full/ckeditor.js"></script>
    <%}else{}  %>
</head>

<body class="">
  <div class="wrapper ">
    <div class="sidebar" data-color="purple" data-background-color="white" data-image="#">
      <div class="logo">
        <a href="#" class="simple-text logo-normal">
          Phóng viên - Writer
        </a>
      </div>
      <div class="sidebar-wrapper">
        <ul class="nav">
          <li class="nav-item <% if( typeof(activeAdmin) != 'undefined' && activeAdmin === true){%> active <%}else{}  %>">
            <a class="nav-link" href="../writer/admin">
              <i class="material-icons">dashboard</i>
              <p>Trang chủ</p>
            </a>
          </li>
          <li class="nav-item <% if( typeof(activeUpdate) != 'undefined' && activeUpdate === true){%> active <%}else{}  %>">
            <a class="nav-link" href="../writer/update">
              <i class="material-icons">person</i>
              <p>Cập nhật thông tin</p>
            </a>
          </li>
          <li class="nav-item <% if( typeof(activePost) != 'undefined' && activePost === true){%> active <%}else{}  %>">
            <a class="nav-link" href="../writer/post">
              <i class="material-icons">library_books</i>
              <p>Đăng bài viết</p>
            </a>
          </li>
          <li class="nav-item  <% if( typeof(activeWaitPublish) != 'undefined' && activeWaitPublish === true){%> active <%}else{}  %>">
            <a class="nav-link" href="../writer/waitPublish">
              <i class="material-icons">content_paste</i>
              <p>Bài viết chờ xuất bản</p>
            </a>
          </li>
          <li class="nav-item <% if( typeof(activePublish) != 'undefined' && activePublish === true){%> active <%}else{}  %>">
            <a class="nav-link" href="../writer/published">
              <i class="material-icons">content_paste</i>
              <p>Bài viết đã xuất bản</p>
            </a>
          </li>
          <li class="nav-item <% if( typeof(activeReject) != 'undefined' && activeReject === true){%> active <%}else{}  %>">
            <a class="nav-link" href="../writer/rejected">
              <i class="material-icons">content_paste</i>
              <p>Bài viết bị từ chối</p>
            </a>
          </li>
          <li class="nav-item <% if( typeof(activeWaitReview) != 'undefined' && activeWaitReview === true){%> active <%}else{}  %>">
            <a class="nav-link" href="../writer/waitReview">
              <i class="material-icons">content_paste</i>
              <p>Bài viết chờ duyệt</p>
            </a>
          </li>
          <li class="nav-item <% if( typeof(activeEdit) != 'undefined' && activeEdit === true){%> active <%}else{}  %>">
            <a class="nav-link" href="../writer/edit">
              <i class="material-icons">edit</i>
              <p>Hiệu chỉnh bài viết</p>
            </a>
          </li>

        </ul>
      </div>
    </div>
    <div class="main-panel">
      <!-- Navbar -->
      <nav class="navbar navbar-expand-lg navbar-transparent navbar-absolute fixed-top ">
        <div class="container-fluid">
          <div class="navbar-wrapper">
            <a class="navbar-brand" href="#pablo">
            <% if( typeof(activeAdmin) != 'undefined' && activeAdmin === true){%> Trang chủ <%}else{}  %>
            <% if( typeof(activeUpdate) != 'undefined' && activeUpdate === true){%> Cập nhật thông tin <%}else{}  %>
            <% if( typeof(activePost) != 'undefined' && activePost === true){%> Đăng bài viết <%}else{}  %>
            <% if( typeof(activeWaitPublish) != 'undefined' && activeWaitPublish === true){%> Bài viết chờ xuất bản <%}else{}  %>
            <% if( typeof(activePublish) != 'undefined' && activePublish === true){%> Bài viết đã xuất bản <%}else{}  %>
            <% if( typeof(activeReject) != 'undefined' && activeReject === true){%> Bài viết bị từ chối <%}else{}  %>
            <% if( typeof(activeWaitReview) != 'undefined' && activeWaitReview === true){%> Bài viết chờ duyệt <%}else{}  %>
            <% if( typeof(activeEdit) != 'undefined' && activeEdit === true){%> Hiệu chỉnh bài viết <%}else{}  %>
            </a>
          </div>
          <button class="navbar-toggler" type="button" data-toggle="collapse" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
            <span class="sr-only">Toggle navigation</span>
            <span class="navbar-toggler-icon icon-bar"></span>
            <span class="navbar-toggler-icon icon-bar"></span>
            <span class="navbar-toggler-icon icon-bar"></span>
          </button>
          <div class="collapse navbar-collapse justify-content-end">
            <form class="navbar-form">
              <div class="input-group no-border">
                <input type="text" value="" class="form-control" placeholder="Search..." id="myInput" 
                <% if( typeof(activeUpdate) != 'undefined' && activeUpdate === true){%> disabled <%}else{}  %> 
                onkeyup="searchTable()">
              </div>
            </form>
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="#pablo">
                  <i class="material-icons">dashboard</i>
                  <p class="d-lg-none d-md-block">
                    Stats
                  </p>
                </a>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  <i class="material-icons">notifications</i>
                  <span class="notification">5</span>
                  <p class="d-lg-none d-md-block">
                    Some Actions
                  </p>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                  <a class="dropdown-item" href="#">Mike John responded to your email</a>
                  <a class="dropdown-item" href="#">You have 5 new tasks</a>
                  <a class="dropdown-item" href="#">You're now friend with Andrew</a>
                  <a class="dropdown-item" href="#">Another Notification</a>
                  <a class="dropdown-item" href="#">Another One</a>
                </div>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link" href="#pablo" id="navbarDropdownProfile" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  <i class="material-icons">person</i>
                  <p class="d-lg-none d-md-block">
                    Account
                  </p>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownProfile">
                  <a class="dropdown-item" href="#">Profile</a>
                  <a class="dropdown-item" href="#">Settings</a>
                  <div class="dropdown-divider"></div>
                  <a class="dropdown-item" href="#">Log out</a>
                </div>
              </li>
            </ul>
          </div>
        </div>
      </nav>
      <!-- End Navbar -->
      <div class="content">
        <%- include(filename) %>
      </div>
      <footer class="footer">
        <div class="container-fluid">
          <p style="margin-left: 0 auto;">Copyright&copy;2019 - We are Web8-Team</p>
        </div>
      </footer>
    </div>
  </div>

 <!--   Core JS Files   -->
 <script src="../../public/vendors/js/writer/jquery.min.js"></script>
 <script src="../../public/vendors/js/writer/popper.min.js"></script>
 <script src="../../public/vendors/js/writer/bootstrap-material-design.min.js"></script>
 <script src="../../public/vendors/js/writer/perfect-scrollbar.jquery.min.js"></script>
 <!-- Control Center for Material Dashboard: parallax effects, scripts for the example pages etc -->
 <script src="../../public/vendors/js/writer/material-dashboard.js?v=2.1.1" type="text/javascript"></script>

  <script>
    $(document).ready(function() {
      $().ready(function() {
        $sidebar = $('.sidebar');

        $sidebar_img_container = $sidebar.find('.sidebar-background');

        $full_page = $('.full-page');

        $sidebar_responsive = $('body > .navbar-collapse');

        window_width = $(window).width();

        fixed_plugin_open = $('.sidebar .sidebar-wrapper .nav li.active a p').html();

        if (window_width > 767 && fixed_plugin_open == 'Dashboard') {
          if ($('.fixed-plugin .dropdown').hasClass('show-dropdown')) {
            $('.fixed-plugin .dropdown').addClass('open');
          }

        }

        $('.switch-sidebar-image input').change(function() {
          $full_page_background = $('.full-page-background');

          $input = $(this);

          if ($input.is(':checked')) {
            if ($sidebar_img_container.length != 0) {
              $sidebar_img_container.fadeIn('fast');
              $sidebar.attr('data-image', '#');
            }

            if ($full_page_background.length != 0) {
              $full_page_background.fadeIn('fast');
              $full_page.attr('data-image', '#');
            }

            background_image = true;
          } else {
            if ($sidebar_img_container.length != 0) {
              $sidebar.removeAttr('data-image');
              $sidebar_img_container.fadeOut('fast');
            }

            if ($full_page_background.length != 0) {
              $full_page.removeAttr('data-image', '#');
              $full_page_background.fadeOut('fast');
            }

            background_image = false;
          }
        });

        $('.switch-sidebar-mini input').change(function() {
          $body = $('body');

          $input = $(this);

          if (md.misc.sidebar_mini_active == true) {
            $('body').removeClass('sidebar-mini');
            md.misc.sidebar_mini_active = false;

            $('.sidebar .sidebar-wrapper, .main-panel').perfectScrollbar();

          } else {

            $('.sidebar .sidebar-wrapper, .main-panel').perfectScrollbar('destroy');

            setTimeout(function() {
              $('body').addClass('sidebar-mini');

              md.misc.sidebar_mini_active = true;
            }, 300);
          }

          // we simulate the window Resize so the charts will get updated in realtime.
          var simulateWindowResize = setInterval(function() {
            window.dispatchEvent(new Event('resize'));
          }, 180);

          // we stop the simulation of Window Resize after the animations are completed
          setTimeout(function() {
            clearInterval(simulateWindowResize);
          }, 1000);

        });
      });
    });
  </script>
  <script>
    $(document).ready(function() {
      // Javascript method's body can be found in assets/js/demos.js
      md.initDashboardPageCharts();

    });
  </script>
  <% for(var i=0; i < jsfiles.length; i++){%>
    <% var path = '../../public/resource/js/' + jsfiles[i] + '.js'; %>
    <script src="<%=path%>"></script>
  <%} %>

</body>

</html>
